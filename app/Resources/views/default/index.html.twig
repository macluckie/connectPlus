{% extends 'base.html.twig' %}
{% block body %}
   {{ render(controller('AppBundle\\Controller\\DefaultController::navbarAction')) }}



    {% if messages is defined %}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            {% endfor %}
        {% endfor %}
    {% endif %}

 



<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
  </ol>
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100 imgcarrousel" src="/upload/{{ pictureRamdom1 }}" alt="First slide" height="600">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100 imgcarrousel" src="/upload/{{ pictureRamdom2 }}" alt="Second slide" height="600">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100 imgcarrousel" src="/upload/{{ pictureRamdom3 }}" alt="Third slide" height="600">
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>




<section class="descriptionconnect">
  <h2> Actu</h2>
    <hr>
  <p class="connectPlusDetails">

  {{details | raw}}
  </p>
</section>


    <h2 class="titrejeux">les 3 derniers jeux</h2>
    <section class="dernierjeux">

          <div class="row nouveaujeux">
          {% for game in games %}
            <div class="card" style="width: 18rem;">
              <img class="card-img-top" src="/upload/{{ game.imageName }}" alt="Card image cap" height= 200px>
              <div class="card-body">
                <h5 class="card-title">{{ game.name }}</h5>
                <p class="card-text">{{ game.commentaire }}</p>
                <a href="{{ path('gamedetails', {'id': game.id}) }}" class="btn btn-info">Description</a>
              </div>
            </div>
          {% endfor %}
          </div>

      </section>



    
{{ render(controller('AppBundle\\Controller\\FooterController::getPhoneNumberAction')) }}

{{ render(controller('AppBundle\\Controller\\FormResaController:formAction')) }}

{% endblock %}
